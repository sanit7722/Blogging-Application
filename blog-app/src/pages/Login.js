import Base from "../components/Base";

const Login=()=>{

    return(
        <Base>
        <div>
             <h1>this is login page</h1>

        </div>
        </Base>
    );
};

export default Login;